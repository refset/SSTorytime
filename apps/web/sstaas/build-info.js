// Overwritten at build time by apps/web/build.sh with the real commit
// sha and timestamp. The stub is what runs under `python3 -m http.server`
// straight out of the source tree.
export const BUILD = {
  commit: "dev",
  builtAt: null,
};
